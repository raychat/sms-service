/**
    new sms gateway
    Author : developer name
**/

// for make http or https requests
const request = require('request')

/** 
  @param {string} auth
  @param {string} message
  @param {string} sender
  @param {string} receptor
  @param {function} callback
**/

module.exports.sendSms = (auth, message, sender, receptor, callback) => {
    
    /**
     * write send sms codes for your new sms gateway 
     */

    },

    /** 
      @param {string} apikey
      @param {function} callback
    **/

    module.exports.getInfo = (apikey, callback) => {
        /**
         * write get info codes for your new sms gateway
         */
    }
